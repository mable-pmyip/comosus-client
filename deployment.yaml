apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-comosus
  labels:
    app: client-comosus
spec:
  selector:
    matchLabels:
        app: client-comosus
  replicas: 3
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: client-comosus
    spec:
      containers:
        - name: comosus-client
          image: 947036392937.dkr.ecr.us-east-1.amazonaws.com/comosus-client-img:latest
          ports:
            - containerPort: 3000
              protocol: TCP
          imagePullPolicy: Always